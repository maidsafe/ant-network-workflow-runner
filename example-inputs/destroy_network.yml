# required
network-name: DEV-02
# optional
# A --version argument can also be used to run testnet-deploy with a specific version
testnet-deploy-args: --branch experimental-feature --repo-owner maidsafe
